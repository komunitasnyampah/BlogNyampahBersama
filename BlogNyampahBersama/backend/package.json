{
  "name": "nama-aplikasi-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite-node peladen/index.ts", // Sesuaikan path jika server entry point berubah
    "build": "vite build && vite-node peladen/index.ts --platform=node --package-external --no-bundle --no-minify src/index.ts", // Sesuaikan path
    "preview": "vite preview && vite-node peladen/index.ts",
    "drizzle-kit": "drizzle-kit" // Script Drizzle jika ada
  },
  "dependencies": {
    "express": "^4.18.2", // Pastikan versi Express Anda
    "drizzle-orm": "^0.28.6",
    // Tambahkan dependensi backend lainnya (misal: dotenv, pg, mysql2, etc.)
  },
  "devDependencies": {
    "@types/express": "^4.17.18", // Types untuk Express
    "@types/node": "^20.6.2",
    "drizzle-kit": "^0.19.13",
    "typescript": "^5.2.2",
    "vite": "^4.4.9",
    "vite-node": "^0.34.4"
  }
}
